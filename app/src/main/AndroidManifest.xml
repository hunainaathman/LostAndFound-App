<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hunaina.lostandfound">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterLostItem"
            android:label="Register Lost Item" />
        <activity
            android:name=".ReportFoundItem"
            android:label="Report Found Item" />
        <activity
            android:name=".RetrieveLostItem"
            android:label="Retrieve Lost Item" />
        <activity
            android:name=".Faq"
            android:label="Frequently Asked Questions" />
        <activity
            android:name=".Askedqn"
            android:label="Post a Question" />
    </application>

</manifest>